<h1><%= @category.title %></h1>

<ul>
<% @category.items.each do |item| %>
  <p><%= item.title %> | <%= number_to_currency(item.printable_price) %> | <%= button_to 'Add to Cart', line_items_path(item_id: item.id) if user_signed_in? %></p>
<% end %>
</ul>